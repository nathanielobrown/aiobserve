"""The suite, mirroring the package layout, with recorded sessions for fixtures.

A package so modules can share scaffolding from `tests.conftest`; `gallery/` serves the same
fixtures as pages for UI work (`docs/ui-development.md`).
"""

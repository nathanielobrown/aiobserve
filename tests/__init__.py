"""The suite, mirroring the package layout; fixtures are recorded sessions, and `gallery/`
serves them as pages (`docs/ui-development.md`).

A package so modules can share scaffolding from `tests.conftest`.
"""

"""The trace viewer: a local web app serving every node of a session as its own page.

What each page shows, and the URL to cite it by, is `docs/viewer.md`; the conventions a
component holds to are in `.claude/rules/viewer-ui.md`.
"""

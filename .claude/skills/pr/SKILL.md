---
name: pr
description: Open a reviewable PR end to end — docs synced into the same PR, house-style description, gh pr create. Invoke when a task should finish as a PR.
---

# PR

Open a reviewable PR by the flow in `docs/pull-requests.md` (imported below): shape the branch into atomic commits, sync the docs, write the description in house style, then push once and open the PR with `gh`. Take the commands from the guide rather than habit.

@../../../docs/pull-requests.md
